<div align="center">
  <%= paginate @comments %>
</div>
<% comments.each do |comment| %>
  <%= render(partial: 'comments/comment', object: comment) %>
<% end %>
<div align="center">
  <%= paginate @comments %>
</div>